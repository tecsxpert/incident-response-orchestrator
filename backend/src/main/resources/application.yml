spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tooldb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false   # ✅ fixes warning

  flyway:
    enabled: true
    locations: classpath:db/migration